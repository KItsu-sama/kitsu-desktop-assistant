# Placeholder LoRA Adapters

This folder contains placeholder adapter directories used for tests and
local development when real LoRA adapters are not available. Each
subfolder (e.g., `chaotic`, `sweet`) is intentionally empty and serves
as a marker so the system can discover available styles.
